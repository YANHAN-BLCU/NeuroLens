# NeuroLens2 核心依赖
# 安装: pip install -r requirements.txt
# GPU 版本请先安装 CUDA 版 PyTorch: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

# ── Web 框架 ──
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# ── ML / 推理 ──
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.30.0
tokenizers>=0.19.0

# ── GPU 量化 (可选，8B 模型需要) ──
bitsandbytes>=0.43.0

# ── 数值计算 ──
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ── 数据处理 ──
datasets>=2.14.0
pandas>=2.0.0

# ── 模型下载 ──
modelscope>=1.14.0

# ── 可视化后端 ──
httpx>=0.24.0
requests>=2.31.0
jinja2>=3.1.0
websockets>=11.0

# ── 桌面窗口 (打包用) ──
pywebview>=4.0.0

# ── 打包工具 ──
# pyinstaller>=6.0.0  # 手动安装，不自动升级
