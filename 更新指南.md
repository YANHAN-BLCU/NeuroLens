生成应用 exe（产物输出到项目根目录\dist）

```bash
pyinstaller visualization\backend\NeuroLens.spec --noconfirm
```



生成安装包 exe

```bash
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ".\installer\NeuroLens_Setup.iss"
```

**注意：** 安装脚本打包仓库根目录下的 `NeuroLens.exe`、`_internal\` 与 `models\`。两个模型目录均不做 Excludes 排除。
